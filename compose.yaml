services:
  postgres:
    image: postgres:16
    container_name: summit-postgres
    environment:
      POSTGRES_USER: summit
      POSTGRES_PASSWORD: summit
      POSTGRES_DB: summit
    ports:
      - "5433:5432"
    volumes:
      - summit_postgres_data:/var/lib/postgresql/data

volumes:
  summit_postgres_data:
